Options -MultiViews
RewriteEngine On
RewriteBase /

# Si el archivo existe, servirlo directo (css, js, imágenes, etc.)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Todo lo demás → index.html
RewriteRule ^ index.html [L]
